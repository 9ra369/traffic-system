// ════════════════════════════════════════════════════
// 追加先: 既存 STATE (DOP)
// ════════════════════════════════════════════════════

// 前車比較に追加
if (i@yield_conflict == 1)
    nearest_stop = min(nearest_stop, f@dist_to_conflict);

// 制御ソースとして追加（歩行者判定の直後）
if (i@yield_conflict == 1 && f@dist_to_conflict < f@dist_to_target) {
    f@dist_to_target = f@dist_to_conflict;
    ctrl_state = 2;      // 赤信号と同じ「止まって待つ」
    i@ctrl_src = 3;
}
